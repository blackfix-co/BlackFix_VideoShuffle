# BlackFix VideoShuffle

Transparent Windows desktop link launcher built with C++ and Win32.

## Build

```powershell
cmake -S . -B build -G "Visual Studio 18 2026" -A x64
cmake --build build --config Release
```
